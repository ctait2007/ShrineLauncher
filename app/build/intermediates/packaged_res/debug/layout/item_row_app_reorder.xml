<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingVertical="6dp"
    android:paddingHorizontal="4dp"
    android:focusable="false"
    android:descendantFocusability="afterDescendants">

    <CheckBox
        android:id="@+id/cbReorderVisible"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:buttonTint="@color/accent"
        android:focusable="false"
        android:layout_marginEnd="8dp" />

    <ImageView
        android:id="@+id/ivReorderIcon"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:scaleType="fitCenter"
        android:focusable="false"
        android:layout_marginEnd="10dp" />

    <!-- App name — this is the main focusable item for toggling -->
    <TextView
        android:id="@+id/tvReorderName"
        android:layout_width="0dp"
        android:layout_height="48dp"
        android:layout_weight="1"
        android:textColor="@color/text_primary"
        android:textSize="13sp"
        android:maxLines="1"
        android:ellipsize="end"
        android:gravity="center_vertical"
        android:focusable="true"
        android:focusableInTouchMode="false"
        android:background="@drawable/selector_focusable_item" />

    <!-- Up / Down reorder buttons — independently focusable -->
    <ImageButton
        android:id="@+id/btnMoveUp"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:src="@drawable/ic_arrow_back"
        android:rotation="90"
        android:background="@drawable/selector_focusable_item"
        android:tint="@color/text_secondary"
        android:contentDescription="Move up"
        android:focusable="true"
        android:focusableInTouchMode="false"
        android:padding="8dp" />

    <ImageButton
        android:id="@+id/btnMoveDown"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:src="@drawable/ic_arrow_back"
        android:rotation="270"
        android:background="@drawable/selector_focusable_item"
        android:tint="@color/text_secondary"
        android:contentDescription="Move down"
        android:focusable="true"
        android:focusableInTouchMode="false"
        android:padding="8dp" />

</LinearLayout>
