<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Granted once via ADB: adb shell pm grant com.shrine.launcher android.permission.WRITE_SECURE_SETTINGS -->
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Granted via ADB for Force Stop: adb shell pm grant com.shrine.launcher android.permission.FORCE_STOP_PACKAGES -->
    <!-- Granted via ADB for Silent Install: adb shell pm grant com.shrine.launcher android.permission.INSTALL_PACKAGES -->
    <uses-permission android:name="android.permission.FORCE_STOP_PACKAGES" />
    <uses-permission android:name="android.permission.INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission"/>

    <!-- TvContract / WatchNext permissions -->
    <uses-permission android:name="com.android.providers.tv.permission.READ_EPG_DATA" />

    <uses-permission android:name="android.permission.READ_TV_LISTINGS" />

    <!-- Required to be treated as a launcher and get WatchNext access on Fire TV -->
    <uses-permission android:name="android.permission.CHANGE_HDMI_CEC_ACTIVE_SOURCE" tools:ignore="ProtectedPermissions"/>

    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.software.leanback" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShrineLauncher"
        android:banner="@drawable/banner">

        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="keyboard|keyboardHidden|navigation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.home.HomeActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:configChanges="keyboard|keyboardHidden|navigation"
            android:windowSoftInputMode="stateUnspecified|adjustPan" />

        <activity android:name=".ui.settings.SettingsActivity"
            android:exported="false" android:label="@string/settings_title" />
        <activity android:name=".ui.settings.RowEditorActivity"
            android:exported="false" android:label="@string/row_editor_title" />
        <activity android:name=".ui.settings.AppPickerActivity"
            android:exported="false" android:label="@string/app_picker_title" />

        <activity android:name=".ui.installer.AppInstallerActivity"
            android:exported="false"
            android:label="App Installer" />

        <activity android:name=".ui.settings.ShrineSettingsActivity"
            android:exported="false" android:label="Shrine Settings" />
        <activity android:name=".ui.settings.GeneralSettingsActivity"
            android:exported="false" android:label="General" />
        <activity android:name=".ui.settings.AppearanceSettingsActivity"
            android:exported="false" android:label="Appearance" />
        <activity android:name=".ui.settings.CategoryAppearanceActivity"
            android:exported="false" android:label="Category Appearance" />
        <activity android:name=".ui.settings.CardAppearanceActivity"
            android:exported="false" android:label="Card Appearance" />
        <activity android:name=".ui.settings.WallpaperPickerActivity"
            android:exported="false"
            android:theme="@style/Theme.Transparent"
            android:label="Pick Wallpaper" />
        <activity android:name=".ui.settings.WallpaperAppearanceActivity"
            android:exported="false" android:label="Wallpaper" />
        <activity android:name=".ui.settings.StatusBarSettingsActivity"
            android:exported="false" android:label="Status Bar" />
        <activity android:name=".ui.settings.IdleModeSettingsActivity"
            android:exported="false" android:label="Idle Mode" />
        <activity android:name=".ui.settings.ManageSettingsActivity"
            android:exported="false" android:label="Manage Settings" />

        <receiver android:name=".ui.installer.InstallResultReceiver" android:exported="false">
            <intent-filter>
                <action android:name="com.shrine.launcher.INSTALL_RESULT" />
            </intent-filter>
        </receiver>

        <!-- FileProvider for sharing APK files with system installer -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

        <receiver android:name=".util.BootReceiver" android:exported="true">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Handles user dismissing a program card from the home screen -->
        <receiver android:name=".util.ProgramRemovedReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.media.tv.ACTION_PREVIEW_PROGRAM_BROWSABLE_DISABLED" />
                <action android:name="android.media.tv.ACTION_WATCH_NEXT_PROGRAM_BROWSABLE_DISABLED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
